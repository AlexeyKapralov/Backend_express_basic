import { IUserInputModel } from '../features/users/models/user.input.model'
import { usersService } from './users.service'

export const blogsService = {
	async createBlog(body: IUserInputModel) {}
}
